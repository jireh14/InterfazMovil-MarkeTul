<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marketul">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Proceso_Pago"></activity>
        <activity android:name=".Direccion_Envio" />
        <activity android:name=".Carrito_Compras" />
        <activity android:name=".Lista_Catalogos" />
        <activity android:name=".Configuracion_Cuenta" />
        <activity android:name=".Categorias" />
        <activity android:name=".Registro" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/iniciomarketul">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>